%h2= t('.title')

- if @data.need_data? and @data.data
  - form_for @data do |f|
    %p= t('.please_supply_user_map')
    %table#map_users
      %tr
        %th= t('.user')
        %th= t('.teambox_user')
        %th
      = render :partial => 'user_map', 
               :collection => @data.map_data.map{|key,value|[key,value]}, 
               :locals => {:users => @data.users_lookup}
    .submit
      = f.submit t('.submit')
      = t('common.or')
      %a{:href => '#', :class => 'cancel'}= t('common.cancel')
- elsif @data.processed_at
  %p= t('datas.data.processed_at', :processed_at => @data.processed_at)
- else
  %p= t('.import_error')
